spring:
  cloud:
    config:
      profile: dev
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:h2:mem:tripbooking
    username: tripbooking
    password:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    defer-datasource-initialization: true